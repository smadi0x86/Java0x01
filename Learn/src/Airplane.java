public abstract class Airplane {
    // * This works simultaneously with "Helicopter.java" and "Abstraction.java" files.

    abstract void go(); // ? This is an abstract method, it does not have a body, it is only declared.
    // ? This forces us to implement the method in the subclass of the abstract class.
}
