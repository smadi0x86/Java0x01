/*
    Writer's notes
    ======================
    ! It is recommended to have the "Better Comments" extension while reading this file.


*/

/*
    Explaining App.java
    ======================
    ! This is the main method that is called when you run the program and then it calls all the other methods.
    ! Uncomment the method that you want to run and comment the rest.

*/

/*
    How to use App.java
    ======================
    ! Uncomment any of the lines below in order to test the classes in the other files.

*/

public class App {
    public static void main(String[] args) throws Exception {

        // Variables.learn(args); // ? args is an array of strings that you can pass to the method

        // StringMethods.learn(args);

        // UserInput.learn(args);

        // MathMethods.learn(args);

        // Arrays.learn(args);

        // Gui.learn(args);

        // WrapperClasses.learn(args);

        // ArrayLists.learn(args);

        // MultiDimensionArrayList.learn(args);

        // ForEach.learn(args);

        // Methods.learn(args);

        // OverloadedMethods.learn(args);

        // Printf.learn(args);

        // * OOP section:

        // Objects.learn(args);

        // Constructors.learn(args);

        // VariableScope.learn(args);

        // OverloadedConstructors.learn(args);

        // ToString.learn(args);

        // ArrayOfObjects.learn(args);

        // ObjectPassing.learn(args);

        // StaticKeyword.learn(args);

        // Inheritance.learn(args);
    }
}
