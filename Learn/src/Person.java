public class Person {
    // * This works simultaneously with "Hero.java" and "SuperKeyword.java" files.

    String name;
    int age;

    // ? This is a constructor, which is a special method that is called when an object of a class is created.\
    // ? It's job is to assign name and age to the variables.
    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}

